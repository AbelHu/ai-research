# Secrets / local environment
.env
.env.*
!.env.example

# Local data (sqlite db, vector index, blobs, reports)
data/

# Test + run logs (detailed, secret-free; regenerated each run)
logs/
backend/logs/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / frontend
node_modules/
dist/
build/

# Vendored offline wheel cache (large, platform-specific binaries).
# Not pushed to the remote; rebuild locally with scripts/fetch-wheels.sh
# (downloads the exact, hash-pinned versions from backend/requirements.lock).
vendor/wheels/

# Editor / OS
.DS_Store
